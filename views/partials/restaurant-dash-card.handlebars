


<div class="card">
<!--Add some dummy details for now -->
    <!--Turn The card into a link that we can click to access indivdually-->
    <a href="/dashboard/{{id}}">
    <h3>{{name}}</h3>
    <p>{{bio}}</p>
    <p>{{location}}</p>
    <p>{{website}}</p>
    <p>rating: {{restaurantRating}}</p>
    </a>    
</div>

{{!-- TODO: one have final css for app, need to update links so retaurant dash card goes to rest page, and add edit button  --}}

<h4>Reviews: </h4>

{{#each Reviews as |review|}}
        {{>review}}
{{/each}}